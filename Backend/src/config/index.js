module.exports = {
    ServerConfig: require('./server-config'), // Importing server configuration from './server-config' module
    Logger: require('./logger-config') // Importing logger configuration from './logger-config' module
};
